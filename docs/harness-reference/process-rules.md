# Process Rules (작업 방식 규칙 대장)

소유자가 AI의 **작업 방식**(결과 보고, 절차, 위임, 확인 요청)에 준 피드백을
일반화해 축적하는 대장이다. 산출물(글)의 품질 선호는
`docs/resume-reference/feedback-rules.md`가 정본이며, 이 대장은 "AI가 어떻게
일하는가"만 담는다. 이 파일은 `CLAUDE.md`가 @import 하므로 여기 실린 내용은
매 세션 컨텍스트에 상주한다 — 간결함이 곧 비용이다.

## 운영 규칙

- **적용 대상**: 모든 에이전트(Claude·Codex 포함) 공유. active와 probation
  모두 따른다(probation은 잠정이되 적용 대상). Claude 하네스에만 해당하는
  규칙은 `- 범위: Claude` 필드로 표시한다. 최상위 에이전트는 서브에이전트에
  위임할 때 그 작업과 관련된 active/probation 규칙을 프롬프트에 요약해
  전달한다.
- **수집**: 일회성 지시("이번엔 짧게 보고해")는 규칙화하지 않는다.
  "앞으로는/항상/매번" 같은 지속성 어휘가 있으면 즉시 후보를 한 줄로
  제안한다. 지속성 어휘 없는 교정은 하단 "관찰 로그"에 한 줄로 남기고, 같은
  교정이 2회째 관찰될 때 후보로 제안한다. 규칙은 소유자가 승인한 것만
  기록한다. 무단 규칙 기록 금지.
- **상태 모델**: 강도 `MUST` / `NEVER` / `PREFER`(어길 땐 이유 명시). 상태
  `probation` / `active` / `retired`. 새 규칙은 `probation`으로 시작한다.
  - "적용 1회" = 규칙의 트리거 상황이 실제 발생했고 재교정 없이 통과한 것.
    세션당 규칙별 최대 1회만 집계하며 `- 적용: YYYY-MM-DD ✓ 무엇으로 확인`
    형식으로 남긴다. 세션의 마지막 응답에서만 발휘된 적용은 소유자가 교정할
    기회가 없었으므로 그 자리에서 집계하지 않고, 다음 세션에서 무교정을
    확인한 뒤 집계한다. ✓ 2회는 승격 가능 시점 표시일 뿐이며, `active`
    승격은 에이전트가 제안하지 않고 소유자가 실제 활용 판단으로 지시한다.
  - 재교정이 오면 성격을 구분한다. **위반**(규칙은 유효한데 에이전트가
    어김) → 규칙은 유지하고 `- 적용: YYYY-MM-DD 위반(요지)`을 기록하며,
    반복 위반 + 기계 검사 가능이면 hook 승격 후보로 올린다.
    **선호 변경**(소유자가 규칙 자체를 뒤집음) → 수정 또는 retire를
    제안한다.
  - 상태 전이는 전부 승인제. **삭제는 없다** — retire 시 헤딩 상태를
    `retired`로 바꾸고 `- 해제: YYYY-MM-DD 이유`를 붙여
    `process-rules-archive.md`로 옮긴다(아카이브는 컨텍스트에 상주하지
    않는다).
- **일반화 우선**: 새 규칙 추가보다 기존 규칙을 일반화해 갱신하는 것이 기본
  동작이다. 추가 시 기존 규칙과의 모순을 검사하고, 충돌하면 어느 쪽이 최신
  취향인지 물어 한쪽을 retire한다.
- **소프트 캡**: active 15개, probation 5개. 추가·승격 시점 모두 캡을
  검사하고, 초과하면 통합·정리 후보를 함께 제안한다.
- **강제 지점**: 규칙은 컨텍스트 상주로 상시 적용하고, 실질적 작업 마무리
  보고 시점에 active/probation 규칙 위반이 없었는지 자가점검한다.
  자가점검에서 위반을 발견하면 최종 응답 전에 고치고, 고칠 수 없으면 위반
  사실을 보고하며 `- 적용: YYYY-MM-DD 위반(요지)`으로 기록한다. 특정 규칙이
  active인데도 반복 위반되고 기계적으로 검사 가능할 때만, 개별적으로
  settings.json hook 승격을 제안한다(`update-config` 스킬 사용).
- **기록 주체**: 규칙 문장·강도·상태의 신설과 변경은 소유자 승인 후 최상위
  에이전트만 수행한다. `적용:` 이력과 관찰 로그는 운영 메타데이터로, 최상위
  에이전트가 승인 없이 기록할 수 있다. 서브에이전트는 이 파일을 편집하지
  않는다.

## 규칙 형식

카테고리: `보고`(결과 전달 방식) / `절차`(작업 순서·확인 타이밍) /
`위임`(서브에이전트·도구 사용) / `확인`(질문·승인 요청 기준).
카테고리 신설은 소유자 승인 필수. ID는 카테고리와 무관한 `P-NN` 순번이며
재사용하지 않는다 — 새 ID를 매기기 전에 아카이브의 마지막 번호까지
확인한다.

```markdown
### P-NN · 보고 · MUST · probation
규칙 문장 한 줄 (일반화된 형태).
- 출처: YYYY-MM-DD 어떤 세션에서
- 적용: YYYY-MM-DD ✓ 무엇으로 확인   (✓ 2회 → 승격 가능, 소유자 지시)
- 범위: Claude                        (선택, 없으면 모든 에이전트)
```

---

## 규칙

### P-01 · 절차 · MUST · probation
이력서/CV 초안은 최종화(designer 렌더·소유자 전달) 전에 대상 회사의
`Screen profile` 프리셋으로 포지셔닝한 `recruiter-screen`+`tech-screen`
게이트를 통과(둘 다 ≥바, 회사 블로커 0)해야 하며, 3라운드 내 미통과 시
점수·블로커를 보고하고 멈춘다. 프리셋 미지정 회사는 Balanced+경고로 채점한다.
- 출처: 2026-07-22 채점관(인사/기술담당자) 게이트 설계 세션
- 적용: 2026-07-22 ✓ 카카오페이 이력서 최종화 전 recruiter-screen(66)·tech-screen(68) 게이트 실행, 둘 다 FAIL을 점수·블로커와 함께 보고하고 정지 — 미달 초안을 통과로 출고하지 않음(사용자가 정직한 상향지원으로 진행 결정)
- 적용: 2026-07-22 ✓ AX본부 이력서를 recruiter(76)·tech(59 캡) 게이트로 2라운드 채점, 구조적 블로커(서버리스 실경험 부재)로 미통과하자 3라운드 강행 대신 점수·블로커 보고·중단
- 적용: 2026-07-23 ✓ 넥스트증권 이력서(인사 72→86·기술 84)·자기소개서(인사 78→80.3·기술 74→81.5) 모두 3라운드 내 둘 다 PASS로 통과 후 출고
- 적용: 2026-07-24 ✓ GS리테일 AX본부 이력서를 recruiter 71·tech 73 게이트로 채점, 구조적 블로커(GCP·DynamoDB·Lambda 실경력 부재, 날조 불가)로 미통과하자 3라운드 강행 대신 점수·블로커 보고·정지, 소유자 "정직한 상향지원" 결정으로 진행(AX본부 전례와 동일 처리)
- 적용: 2026-08-04 ✓ 스마일게이트 AI센터 이력서를 Balanced(80) 게이트로 3라운드 채점(인사 74→75.5→74.5 / 기술 72→77.5→83 PASS), AND 게이트 미통과로 강행 대신 점수·블로커(0건, 잔여 감점은 구조적: 비전공·Java 연수) 보고 후 정지 — 미달 초안을 통과로 출고하지 않음
- 적용: 2026-08-12 ✓ 네이버웹툰 이력서를 Platform-DS(80) 게이트로 3라운드 채점(인사 77→74→79 / 기술 70.5→72→77), AND 게이트 미통과로 강행 대신 점수·블로커(0건, 잔여 감점은 구조적: 은행 무근거 아키텍처 세부·미확정 지표) 보고 후 정지 — 미달 초안을 통과로 출고하지 않음
- 적용: 2026-08-14 ✓ 빗썸(빗썸페이) 이력서를 Balanced(80) 게이트로 3라운드 채점(인사 70→75→78 / 기술 66→69.5→71.5), AND 게이트 미통과로 4라운드 강행 대신 점수·블로커(0건, 잔여 감점은 구조적: RN 실무 밀도·동기 문장 미기재 정책) 보고 후 정지 — 소유자 "정직한 상향지원" 결정으로 출고
- 적용: 2026-08-18 ✓ CJ푸드빌(뚜레쥬르 APP 리빌딩) 이력서를 Platform-DS(80) 게이트로 2라운드 채점(인사 74.4→82 PASS / 기술 79→76 FAIL), AND 게이트 미통과가 구조적 갭(디자인 토큰·공통 모듈 아키텍처 근거 부족, 날조 불가)이라 3라운드 강행 대신 점수·블로커(0건) 보고 후 소유자 "정직한 상향지원" 결정으로 출고 — 미달본을 통과로 위장하지 않음
- 적용: 2026-08-21 ✓ GS글로벌 FDE 이력서를 AI-Product(80) 게이트로 3라운드 채점(인사 71→77 / 기술 69.5→75), AND 미통과로 강행 대신 점수·블로커(0건) 보고 후 정지 — 소유자 지시 갭 인터뷰 2회(MCP·데이터 과제화·Python 구체화, 캡 비산입 보강 라운드)로 최종 인사 86 / 기술 81.5 둘 다 PASS 후 출고
- 적용: 2026-08-25 ✓ 코오롱베니트 플랫폼 개발/운영 이력서를 Platform-DS(80) 게이트로 3라운드 채점(R1 76/71 → R2 75.5/71 → R3 69.5/77, 블로커 0), 캡 소진 시점마다 강행 대신 점수·블로커 보고 후 정지 — 소유자 지시 갭 인터뷰 3회(캡 비산입 보강 2라운드, 최종 72.5/73.9)로도 구조적 갭(공백 미기재·메신저/웹하드 무경험) 미해소, 소유자 "정직한 상향지원" 결정으로 출고 — 미달본을 통과로 위장하지 않음
- 적용: 2026-08-25 ✓ (별도 세션) 코오롱베니트 써치라인 v3 가독성 개편본을 Platform-DS(80) 게이트로 5라운드 채점(인사 77→73.5→76→73→70 / 기술 67→69→70.5→74.15→72.5, 블로커 전 라운드 0), 라운드마다 강행 대신 점수·구조적 사유 보고 후 정지 — 소유자 지시 갭 인터뷰 3회(캡 비산입: 인덱스·트랜잭션·WebView 사례·아키텍처 대가)로 사실 소진 확인, 소유자 "폴리시 후 출고"(정직한 상향지원) 결정으로 은행 근거 폴리시 3건만 적용해 출고 — 미달본을 통과로 위장하지 않음
- 적용: 2026-09-09 ✓ 지오영 AI 개발 이력서를 Balanced(80, 프리셋 미승인) 게이트로 3라운드 채점(R1 75.5/66 → R2 75/65.5 → R3 68/64.5, 블로커 0) 후 캡 소진 시점에 강행 대신 점수·구조적 사유 보고·정지, 소유자 프레이밍 지시 후 보강 라운드(캡 비산입) 59(블로커 1, 즉시 수정)/63 — 4라운드 모두 미달본을 통과로 위장하지 않고 "문면으로는 80 불가, 본인 명의 산출물이 유일 경로"를 보고. 이후 정돈 개정(v5)은 사실 추가 없는 축약이라 재채점 대신 reviewer 무결성 재검으로 대체하고 그 사실을 보고
- 범위: Claude

### P-02 · 위임 · NEVER · probation
Codex 핑퐁(독립 모델 검증) 루프를 실행하지 않는다. 산출물 하드닝은
same-model `reviewer`/critics와 점수 게이트(`recruiter-screen`·`tech-screen`)로
대신한다.
- 출처: 2026-07-22 카카오페이 지원 세션 ("codex 독립검증은 더이상 이용 안 해")
- 적용: 2026-07-22 ✓ 채점 게이트/하네스 변경 하드닝을 Codex 없이 Claude 서브에이전트 2라운드 적대 검증으로 수행 — 재교정 없이 통과
- 적용: 2026-07-23 ✓ 헤드헌터 어드바이저 하네스(신설 에이전트 3개+스킬+참조파일) 하드닝을 Codex 없이 Claude 서브에이전트 2라운드 적대 검증으로 수행 — 재교정 없이 통과
- 적용: 2026-07-24 ✓ GS리테일 AX 이력서 하드닝(적대검증·크리틱·점수 게이트)을 Codex 없이 Claude 서브에이전트(general-purpose 적대검증 + reviewer/ats + recruiter/tech-screen)로 수행
- 적용: 2026-07-25 ✓ base-resume(마스터 이력서) 전 섹션 자문·하드닝을 Codex 없이 Claude 서브에이전트(recruiter-screen·tech-screen·헤드헌터 페르소나 3종 반복 투입)로 수행 — 재교정 없음
- 적용: 2026-07-27 ✓ base-resume 전면 재작성 하드닝을 Codex 없이 Claude 서브에이전트(general-purpose 적대 검증 + reviewer 사실 검증)로 수행, 지적 70건을 삼분 처리
- 적용: 2026-07-27 ✓ (별도 세션) 자기소개서 범용 마스터 v2 하드닝을 Codex 없이 Claude 서브에이전트(reviewer 사실 검증 + general-purpose 적대 검증 + recruiter/tech-screen 3라운드 게이트)로 수행, 적대 지적 삼분 처리
- 적용: 2026-08-01 ✓ 자기소개서 base 항목 3 하드닝을 Codex 없이 Claude 서브에이전트(reviewer 사실 검증 2회 + recruiter/tech-screen 3라운드 + 헤드헌터 페르소나)로 수행 — 재교정 없음
- 적용: 2026-08-04 ✓ 포트폴리오 3인 자문 평가 리포트 하드닝을 Codex 없이 Claude 서브에이전트 적대 검증 2라운드(1R 10건 수용, 2R critical 0)로 수행. 같은 날 별도 세션에서 스마일게이트 이력서 하드닝도 Codex 없이 Claude 서브에이전트(reviewer 2회 + general-purpose 적대 검증 + recruiter/tech-screen 3라운드)로 수행
- 적용: 2026-08-11 ✓ 포트폴리오 확장판(신규 리드 5개 + 25장 렌더) 하드닝을 Codex 없이 Claude 서브에이전트(reviewer 사실 검증 must-fix 4건 + UX·인사·기술·헤드헌터 4자 평가)로 수행 — 재교정 없음
- 적용: 2026-08-12 ✓ 네이버웹툰 이력서 하드닝을 Codex 없이 Claude 서브에이전트(reviewer 2회 + 적대 검증 2패스 + recruiter/tech-screen 3라운드)로 수행 — 재교정 없음
- 적용: 2026-08-14 ✓ 빗썸 이력서 하드닝을 Codex 없이 Claude 서브에이전트(reviewer 3회 + 적대 검증 2패스 + recruiter/tech-screen 3라운드)로 수행 — 재교정 없음
- 적용: 2026-08-18 ✓ CJ푸드빌 이력서 하드닝을 Codex 없이 Claude 서브에이전트(reviewer 2회+최종 무결성 재검, general-purpose 적대 검증, recruiter/tech-screen 2라운드 게이트)로 수행 — 재교정 없음
- 적용: 2026-08-21 ✓ GS글로벌 FDE 이력서·지정양식 docx 하드닝을 Codex 없이 Claude 서브에이전트(reviewer 3회+최종 무결성 재검 2회, general-purpose 적대 검증, recruiter/tech-screen 게이트 6라운드)로 수행 — 재교정 없이 통과
- 적용: 2026-08-25 ✓ 코오롱베니트 이력서 하드닝을 Codex 없이 Claude 서브에이전트(reviewer 무결성 전수 대조 2회, ats, general-purpose 적대 검증, recruiter/tech-screen 게이트 5라운드)로 수행 — 재교정 없이 통과 (writer 529 중단으로 초안은 최상위 직접 재조립, 검증 체인은 서브에이전트 유지)
- 적용: 2026-08-25 ✓ (별도 세션) 코오롱베니트 써치라인 v2 템플릿 충실 재조립 하드닝을 Codex 없이 Claude 서브에이전트 적대 검증 2라운드(1R 지적 11건 삼분 처리, 2R CRITICAL 0)로 수행 — 재교정 없이 통과
- 적용: 2026-09-09 ✓ 지오영 AI 개발 이력서 하드닝(v1~v5)을 Codex 없이 Claude 서브에이전트로 수행 — reviewer 5회·general-purpose 적대 검증 3회·recruiter/tech-screen 8회·Explore 저장소 조사 1회·archivist 적재 3회
- 범위: Claude

### P-03 · 위임 · PREFER · probation
이력서/CV의 새 섹션·항목 초안은 소유자 확정 전에 3자 자문 리뷰
(recruiter-screen·tech-screen·헤드헌터 페르소나 중 항목 성격에 맞는 조합)를
기본으로 제안한다. 소유자가 생략을 원하면 따른다.
- 출처: 2026-07-25 base-resume 세션 — 같은 지시("인사담당자·기술담당자·
  헤드헌터에게 리뷰 받아봐") 5회 반복 후 소유자 승인
- 적용: 2026-08-01 ✓ 자기소개서 base 항목 3 신규 초안에 3자 자문(reviewer/
  recruiter-screen/tech-screen, v3부터 소유자 지정 "인사·기술·헤드헌터"
  조합)을 라운드마다 실행 — 재교정 없음
- 범위: Claude

### P-04 · 절차 · PREFER · probation
세션 중 도구·파이프라인 장애를 2회 이상 재시도로 해결했으면, 세션 마무리
전에 `docs/troubleshooting/` 적재 여부를 자가점검하고 미적재분을 표준
순서(재현→확인→검증→해결→재발 방지)로 기록한다. 에이전트 스스로 해결한
장애는 제보 트리거가 없어 휘발되기 쉽다는 것이 신설 사유.
- 출처: 2026-08-25 코오롱베니트 v4 세션 — "이런 트러블슈팅이 하네스
  엔지니어링 측면에서 축적되고 있어?" 지적 후 docx 파이프라인 6건 소급
  적재, 규칙 후보 소유자 승인
- 적용: 2026-09-09 ✓ 지오영 세션에서 Word AppleScript 변환이 소유자 Word 세션 점유로 3회 연속 무한 대기(-1712)한 장애를 자가 해결한 뒤, 제보 없이 `docs/troubleshooting/docx-template-render-pipeline.md` §7로 적재(재현·확인·검증·해결·재발 방지 표준 순서)
- 범위: Claude

## 관찰 로그 (규칙 후보 전 단계)

지속성 어휘 없이 온 프로세스 교정을 `- YYYY-MM-DD 교정 요지` 한 줄로
기록한다(승인 불요). 같은 교정이 2회째 보이면 규칙 후보로 제안한다. 규칙이
아니므로 삭제 가능하다 — 규칙화됐거나 60일이 지난 항목은 지운다.

- 2026-07-22 게이트 블로커 해소 시 검증된 성과(Lighthouse 91점)를 제거로 처리 — 스코프 병기로 살리는 쪽을 먼저 시도했어야. 소유자 "왜 뺐니" 교정으로 복원.
- 2026-07-23 헤드헌터 인터뷰 질문을 구조화된 번호 목록으로 제시 → 소유자 "문장형으로 다시 줄래" 교정. 이후 질문은 문장형(prose)으로 전달. (2회째 관찰 시 보고 규칙 후보 제안)
- 2026-07-27 P-01 점수 게이트·P-03 3자 자문 생략 지시("4,5,7 부분은 필요없어") — 일회성으로 처리, 규칙 자체는 유지. 지속성 어휘 없음.
- 2026-07-27 검증 에이전트(적대 검증·reviewer) 지적을 소유자 승인 없이 문면에 반영해 20여 곳을 고쳤다가 "다 내가 적은 기준으로 맞춰, 변경하지말고" 교정으로 전면 되돌림. 앞으로 크리틱 결과는 수용/기각 분류만 제시하고 소유자 판단 후 반영한다. (2회째 관찰 시 절차 규칙 후보 제안)
- 2026-08-14 갭 인터뷰 중 "애매한 부분이 있으면 객관식으로 나에게 제안해" 지시 — 사실 확인이 애매할 때 객관식(선택지) 질문으로 제안. 지속성 어휘 없어 관찰만. (2회째 관찰 시 확인 규칙 후보 제안)
- 2026-08-21 base-resume 경력란 변경 시 "기존 대웅제약 블록을 통합해 그룹 헤더 1블록으로" 재구성하는 선택지만 제시 → 소유자 "기존 AI추진팀은 남겨두고 경력란에 1개 더 추가해달라는 이야기였어" 교정으로 되돌림. 기존 항목을 통합·변형하는 구조 선택지를 낼 때는 "기존 유지 + 신규 추가" 선택지를 함께 제시한다. 지속성 어휘 없어 관찰만. (2회째 관찰 시 확인 규칙 후보 제안)
